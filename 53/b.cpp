#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

bool ok(ll h, ll w) {
    return ((h * 1.0f) / w >= 0.8f && (h * 1.0f) / w <= 1.25f);
}

void solve() {
    ll h, w;
    cin >> h >> w;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 1;
    // cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
